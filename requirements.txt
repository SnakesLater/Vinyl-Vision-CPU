fastapi>=0.100
uvicorn[standard]>=0.20
httpx>=0.24
python-multipart>=0.0.6
transformers>=4.48
torch>=2.5
torchvision>=0.20
Pillow>=10.0
# Note: In Docker, CPU-only torch is installed first via --index-url
# in Dockerfile. Native install pulls CUDA version automatically.
